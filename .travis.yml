language: python
sudo: true
python:
  - "2.7"
env:
  - DISPLAY=:99.0
before_install:
  - sh -e /etc/init.d/xvfb start
  - sudo mkdir /dev/input
# dependencies
install:
  - pip install pyglet configparser
# run tests
script:
  - python fightstick.py
